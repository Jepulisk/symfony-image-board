{% extends "base.html.twig" %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<div class="content">
    <div class="pure-g">
        <div class="pure-u-1-1">
        {% for board in boards %}
            <a href="{{path("board", {name: board.name})}}">{{board.name}}</a>
        {% endfor %}
        </div>
    </div>
</div>

{% endblock %}
