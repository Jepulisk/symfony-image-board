{% extends "base.html.twig" %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<div class="content">
    <div class="pure-g">
        <div class="pure-u">
            <h1>Boards</h1>
            {% for board in boards %}
                <p><a href="{{path("board", {name: board.name})}}">/{{board.name}}/</a></p>
            {% endfor %}
        </div>
    </div>
</div>

{% endblock %}
